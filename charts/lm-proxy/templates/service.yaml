apiVersion: v1
kind: Service
metadata:
  name: {{ include "lm-proxy.fullname" . }}
spec:
  type: ClusterIP
  ports:
    - port: 9090
      targetPort: 9090
      protocol: TCP
  selector:
    app: {{ include "lm-proxy.name" . }}
